import { GRAMMAR_STRUCTURE } from './grammar-structure';
import { GRAMMAR_EXPRESSIONS } from './grammar-expressions';
import { GRAMMAR_LITERALS } from './grammar-literals';

export const CALYNDA_GRAMMAR = GRAMMAR_STRUCTURE + GRAMMAR_EXPRESSIONS + GRAMMAR_LITERALS;
